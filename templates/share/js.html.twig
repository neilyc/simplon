<script>
  var CONSTS = {
    url : {
      booking : {
        getAll : "{{ path('booking_index') }}",
        new : "{{ path('booking_new') }}",
        delete: "{{ path('booking_delete') }}",
      },
      computer : {
        getAll : "{{ path('computer_ajax') }}", 
      }
    }
  }

</script>