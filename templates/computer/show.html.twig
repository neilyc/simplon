{% extends 'base.html.twig' %}

{% block title %}Ordinateur{% endblock %}

{% block body %}
    <h1>Ordinateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ computer.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ computer.title }}</td>
            </tr>
        </tbody>
    </table>

    

    <a href="{{ path('computer_edit', {'id': computer.id}) }}" class="btn btn-info">Editer</a>

    {{ include('computer/_delete_form.html.twig') }}

    <a href="{{ path('computer_index') }}">Retour à la liste</a>
{% endblock %}
